#include "gstsoundbar.h"

inline void render (struct state * state,
                    struct video_info vi,
                    struct audio_info ai,
                    guint32 * vdata,
                    gint16 * adata) {

        
}
